# GENERATED by contract/gen.py -- DO NOT EDIT.
#
# InfoHub passthrough: a CACHING MODBUS PROXY on port B.
#
# WHY THIS IS NEEDED AT ALL
#
# Modbus RTU permits exactly one master. The InfoHub is a master. So is this
# bridge. Wire both to the generator's RS-485 pair and they transmit over each
# other: corrupt frames in both directions, and an InfoHub reporting comm
# faults to your dealer.
#
# The board solves it in hardware -- two transceivers, U3 (IO17/16/4) facing
# the generator and U4 (IO22/23/21) facing the InfoHub -- and this file solves
# it in firmware. We are the sole master on the generator bus. The InfoHub is
# no longer on that bus at all; it talks to US, and we answer as a SLAVE at the
# same address, from the values we just read. It cannot tell the difference.
#
# WHAT IS SERVED
#
# All 75 registers across 0x0000-0x0056, reconstructed from the
# scaled sensor values by inverting each filter's multiply. Reconstruction
# rather than a second raw read of the same addresses: a duplicate read would
# double the traffic on a 9600 baud bus that the field notes already warn
# against saturating. lroundf absorbs the float round trip.
#
# A register we have never read, or one the controller reported as 0xFFFF
# ("sensor not present"), is served as 0xFFFF -- exactly what the InfoHub would
# have seen from the controller itself.
#
# WHAT IS NOT SERVED: WRITES.
#
# The InfoHub can no longer command the generator through this bridge. Remote
# start/stop from the Briggs app will stop working; monitoring and cloud
# reporting continue. This is deliberate. Proxying writes would mean this
# bridge cranking an engine on behalf of a remote request it cannot
# authenticate, using the control path that was explicitly removed from this
# build. If you need remote start, wire the InfoHub directly and run this
# bridge read-only on its own port -- you cannot have both on one pair.

uart:
  - id: rs485_uart_b
    tx_pin: ${uart_b_tx_pin}
    rx_pin: ${uart_b_rx_pin}
    baud_rate: ${modbus_baud}
    parity: ${modbus_parity}
    stop_bits: 1
    data_bits: 8

modbus:
  - id: rs485_bus_b
    uart_id: rs485_uart_b
    role: server
    flow_control_pin: ${flow_control_b_pin}

modbus_server:
  - id: infohub_proxy
    modbus_id: rs485_bus_b
    address: ${modbus_address}
    # Answer, rather than fault, for anything in range we do not model. An
    # exception reply teaches the InfoHub the device is broken; 0xFFFF is the
    # controller's own "not present" value and is what it already handles.
    courtesy_response:
      enabled: true
      register_last_address: 0x0056
      register_value: 0xFFFF
    registers:
      - address: 0x0000
        value_type: U_WORD
        read_lambda: |-
          // Protocol Revision
          if (!id(feat_passthrough).state) return {};
          float v = id(protocol_revision).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v);
      - address: 0x0001
        value_type: U_WORD
        read_lambda: |-
          // Generator L1 Voltage
          if (!id(feat_passthrough).state) return {};
          float v = id(generator_l1_voltage).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v / 0.1f);
      - address: 0x0002
        value_type: U_WORD
        read_lambda: |-
          // Generator L2 Voltage
          if (!id(feat_passthrough).state) return {};
          float v = id(generator_l2_voltage).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v / 0.1f);
      - address: 0x0003
        value_type: U_WORD
        read_lambda: |-
          // Generator L3 Voltage
          if (!id(feat_passthrough).state) return {};
          float v = id(generator_l3_voltage).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v / 0.1f);
      - address: 0x0004
        value_type: U_WORD
        read_lambda: |-
          // Generator L1-L2 Voltage
          if (!id(feat_passthrough).state) return {};
          float v = id(generator_l1_l2_voltage).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v / 0.1f);
      - address: 0x0005
        value_type: U_WORD
        read_lambda: |-
          // Generator L2-L3 Voltage
          if (!id(feat_passthrough).state) return {};
          float v = id(generator_l2_l3_voltage).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v / 0.1f);
      - address: 0x0006
        value_type: U_WORD
        read_lambda: |-
          // Generator L3-L1 Voltage
          if (!id(feat_passthrough).state) return {};
          float v = id(generator_l3_l1_voltage).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v / 0.1f);
      - address: 0x0007
        value_type: U_WORD
        read_lambda: |-
          // Generator L1 Frequency
          if (!id(feat_passthrough).state) return {};
          float v = id(generator_l1_frequency).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v / 0.1f);
      - address: 0x0008
        value_type: U_WORD
        read_lambda: |-
          // Generator L2 Frequency
          if (!id(feat_passthrough).state) return {};
          float v = id(generator_l2_frequency).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v / 0.1f);
      - address: 0x0009
        value_type: U_WORD
        read_lambda: |-
          // Generator L3 Frequency
          if (!id(feat_passthrough).state) return {};
          float v = id(generator_l3_frequency).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v / 0.1f);
      - address: 0x000A
        value_type: U_WORD
        read_lambda: |-
          // L1 Power Factor
          if (!id(feat_passthrough).state) return {};
          float v = id(l1_power_factor).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v / 0.01f);
      - address: 0x000B
        value_type: U_WORD
        read_lambda: |-
          // L2 Power Factor
          if (!id(feat_passthrough).state) return {};
          float v = id(l2_power_factor).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v / 0.01f);
      - address: 0x000C
        value_type: U_WORD
        read_lambda: |-
          // L3 Power Factor
          if (!id(feat_passthrough).state) return {};
          float v = id(l3_power_factor).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v / 0.01f);
      - address: 0x000D
        value_type: U_WORD
        read_lambda: |-
          // Average Power Factor
          if (!id(feat_passthrough).state) return {};
          float v = id(average_power_factor).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v / 0.01f);
      - address: 0x000E
        value_type: U_WORD
        read_lambda: |-
          // Utility L1 Voltage
          if (!id(feat_passthrough).state) return {};
          float v = id(utility_l1_voltage).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v / 0.1f);
      - address: 0x000F
        value_type: U_WORD
        read_lambda: |-
          // Utility L2 Voltage
          if (!id(feat_passthrough).state) return {};
          float v = id(utility_l2_voltage).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v / 0.1f);
      - address: 0x0010
        value_type: U_WORD
        read_lambda: |-
          // Utility L3 Voltage
          if (!id(feat_passthrough).state) return {};
          float v = id(utility_l3_voltage).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v / 0.1f);
      - address: 0x0011
        value_type: U_WORD
        read_lambda: |-
          // Utility L1-L2 Voltage
          if (!id(feat_passthrough).state) return {};
          float v = id(utility_l1_l2_voltage).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v / 0.1f);
      - address: 0x0012
        value_type: U_WORD
        read_lambda: |-
          // Utility L2-L3 Voltage
          if (!id(feat_passthrough).state) return {};
          float v = id(utility_l2_l3_voltage).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v / 0.1f);
      - address: 0x0013
        value_type: U_WORD
        read_lambda: |-
          // Utility L3-L1 Voltage
          if (!id(feat_passthrough).state) return {};
          float v = id(utility_l3_l1_voltage).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v / 0.1f);
      - address: 0x0014
        value_type: U_WORD
        read_lambda: |-
          // Utility L1 Frequency
          if (!id(feat_passthrough).state) return {};
          float v = id(utility_l1_frequency).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v / 0.1f);
      - address: 0x0015
        value_type: U_WORD
        read_lambda: |-
          // Utility L2 Frequency
          if (!id(feat_passthrough).state) return {};
          float v = id(utility_l2_frequency).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v / 0.1f);
      - address: 0x0016
        value_type: U_WORD
        read_lambda: |-
          // Utility L3 Frequency
          if (!id(feat_passthrough).state) return {};
          float v = id(utility_l3_frequency).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v / 0.1f);
      - address: 0x0017
        value_type: U_WORD
        read_lambda: |-
          // Generator L1 Current
          if (!id(feat_passthrough).state) return {};
          float v = id(generator_l1_current).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v / 0.1f);
      - address: 0x0018
        value_type: U_WORD
        read_lambda: |-
          // Generator L2 Current
          if (!id(feat_passthrough).state) return {};
          float v = id(generator_l2_current).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v / 0.1f);
      - address: 0x0019
        value_type: U_WORD
        read_lambda: |-
          // Generator L3 Current
          if (!id(feat_passthrough).state) return {};
          float v = id(generator_l3_current).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v / 0.1f);
      - address: 0x001A
        value_type: U_WORD
        read_lambda: |-
          // L1 Power
          if (!id(feat_passthrough).state) return {};
          float v = id(l1_power).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v / 0.1f);
      - address: 0x001B
        value_type: U_WORD
        read_lambda: |-
          // L2 Power
          if (!id(feat_passthrough).state) return {};
          float v = id(l2_power).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v / 0.1f);
      - address: 0x001C
        value_type: U_WORD
        read_lambda: |-
          // L3 Power
          if (!id(feat_passthrough).state) return {};
          float v = id(l3_power).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v / 0.1f);
      - address: 0x001D
        value_type: U_WORD
        read_lambda: |-
          // Total Power
          if (!id(feat_passthrough).state) return {};
          float v = id(total_power).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v / 0.1f);
      - address: 0x001E
        value_type: U_WORD
        read_lambda: |-
          // Percentage Load
          if (!id(feat_passthrough).state) return {};
          float v = id(percentage_load).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v);
      - address: 0x001F
        value_type: U_WORD
        read_lambda: |-
          // L1 Apparent Power
          if (!id(feat_passthrough).state) return {};
          float v = id(l1_apparent_power).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v / 0.1f);
      - address: 0x0020
        value_type: U_WORD
        read_lambda: |-
          // L2 Apparent Power
          if (!id(feat_passthrough).state) return {};
          float v = id(l2_apparent_power).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v / 0.1f);
      - address: 0x0021
        value_type: U_WORD
        read_lambda: |-
          // L3 Apparent Power
          if (!id(feat_passthrough).state) return {};
          float v = id(l3_apparent_power).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v / 0.1f);
      - address: 0x0022
        value_type: U_WORD
        read_lambda: |-
          // Total Apparent Power
          if (!id(feat_passthrough).state) return {};
          float v = id(total_apparent_power).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v / 0.1f);
      - address: 0x0023
        value_type: U_WORD
        read_lambda: |-
          // L1 Reactive Power
          if (!id(feat_passthrough).state) return {};
          float v = id(l1_reactive_power).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v / 0.1f);
      - address: 0x0024
        value_type: U_WORD
        read_lambda: |-
          // L2 Reactive Power
          if (!id(feat_passthrough).state) return {};
          float v = id(l2_reactive_power).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v / 0.1f);
      - address: 0x0025
        value_type: U_WORD
        read_lambda: |-
          // L3 Reactive Power
          if (!id(feat_passthrough).state) return {};
          float v = id(l3_reactive_power).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v / 0.1f);
      - address: 0x0026
        value_type: U_WORD
        read_lambda: |-
          // Total Reactive Power
          if (!id(feat_passthrough).state) return {};
          float v = id(total_reactive_power).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v / 0.1f);
      - address: 0x0027
        value_type: U_DWORD
        read_lambda: |-
          // Cumulative Energy -- exact 32-bit capture
          if (!id(feat_passthrough).state) return {};
          return (uint32_t) id(raw_dword_0027);
      - address: 0x0029
        value_type: U_DWORD
        read_lambda: |-
          // Cumulative Apparent Energy -- exact 32-bit capture
          if (!id(feat_passthrough).state) return {};
          return (uint32_t) id(raw_dword_0029);
      - address: 0x002B
        value_type: U_DWORD
        read_lambda: |-
          // Cumulative Reactive Energy -- exact 32-bit capture
          if (!id(feat_passthrough).state) return {};
          return (uint32_t) id(raw_dword_002b);
      - address: 0x0033
        value_type: U_WORD
        read_lambda: |-
          // Oil Pressure
          if (!id(feat_passthrough).state) return {};
          float v = id(oil_pressure_bar).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v / 0.1f);
      - address: 0x0034
        value_type: U_WORD
        read_lambda: |-
          // Coolant Temperature
          if (!id(feat_passthrough).state) return {};
          float v = id(coolant_temperature).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v / 0.1f);
      - address: 0x0035
        value_type: U_WORD
        read_lambda: |-
          // Fuel Level
          if (!id(feat_passthrough).state) return {};
          float v = id(fuel_level).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v / 0.1f);
      - address: 0x0036
        value_type: U_WORD
        read_lambda: |-
          // Fuel Volume
          if (!id(feat_passthrough).state) return {};
          float v = id(fuel_volume).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v / 0.1f);
      - address: 0x0038
        value_type: U_WORD
        read_lambda: |-
          // Battery Voltage
          if (!id(feat_passthrough).state) return {};
          float v = id(battery_voltage).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v / 0.1f);
      - address: 0x0039
        value_type: U_WORD
        read_lambda: |-
          // Engine Speed
          if (!id(feat_passthrough).state) return {};
          float v = id(engine_speed).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v);
      - address: 0x003A
        value_type: U_WORD
        read_lambda: |-
          // Number of Starts
          if (!id(feat_passthrough).state) return {};
          float v = id(number_of_starts).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v);
      - address: 0x003B
        value_type: U_WORD
        read_lambda: |-
          // Number of Trips
          if (!id(feat_passthrough).state) return {};
          float v = id(number_of_trips).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v);
      - address: 0x003C
        value_type: U_WORD
        read_lambda: |-
          // Engine Run Time Hours
          if (!id(feat_passthrough).state) return {};
          float v = id(run_time_hours).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v);
      - address: 0x003D
        value_type: U_WORD
        read_lambda: |-
          // Engine Run Time Minutes
          if (!id(feat_passthrough).state) return {};
          float v = id(run_time_minutes).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v);
      - address: 0x0040
        value_type: U_WORD
        read_lambda: |-
          // Alarm Register 1
          if (!id(feat_passthrough).state) return {};
          float v = id(alarm_reg_40).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v);
      - address: 0x0041
        value_type: U_WORD
        read_lambda: |-
          // Alarm Register 2
          if (!id(feat_passthrough).state) return {};
          float v = id(alarm_reg_41).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v);
      - address: 0x0042
        value_type: U_WORD
        read_lambda: |-
          // Alarm Register 3
          if (!id(feat_passthrough).state) return {};
          float v = id(alarm_reg_42).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v);
      - address: 0x0043
        value_type: U_WORD
        read_lambda: |-
          // Alarm Register 4
          if (!id(feat_passthrough).state) return {};
          float v = id(alarm_reg_43).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v);
      - address: 0x0044
        value_type: U_WORD
        read_lambda: |-
          // Alarm Register 5
          if (!id(feat_passthrough).state) return {};
          float v = id(alarm_reg_44).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v);
      - address: 0x0045
        value_type: U_WORD
        read_lambda: |-
          // Alarm Register 6
          if (!id(feat_passthrough).state) return {};
          float v = id(alarm_reg_45).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v);
      - address: 0x0046
        value_type: U_WORD
        read_lambda: |-
          // Alarm Register 7
          if (!id(feat_passthrough).state) return {};
          float v = id(alarm_reg_46).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v);
      - address: 0x0047
        value_type: U_WORD
        read_lambda: |-
          // Alarm Register 8
          if (!id(feat_passthrough).state) return {};
          float v = id(alarm_reg_47).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v);
      - address: 0x0048
        value_type: U_WORD
        read_lambda: |-
          // Alarm Register 9
          if (!id(feat_passthrough).state) return {};
          float v = id(alarm_reg_48).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v);
      - address: 0x0049
        value_type: U_WORD
        read_lambda: |-
          // Alarm Register 10
          if (!id(feat_passthrough).state) return {};
          float v = id(alarm_reg_49).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v);
      - address: 0x004A
        value_type: U_WORD
        read_lambda: |-
          // Alarm Register 11
          if (!id(feat_passthrough).state) return {};
          float v = id(alarm_reg_4a).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v);
      - address: 0x004B
        value_type: U_WORD
        read_lambda: |-
          // Alarm Register 12
          if (!id(feat_passthrough).state) return {};
          float v = id(alarm_reg_4b).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v);
      - address: 0x004C
        value_type: U_WORD
        read_lambda: |-
          // Alarm Register 13
          if (!id(feat_passthrough).state) return {};
          float v = id(alarm_reg_4c).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v);
      - address: 0x004D
        value_type: U_WORD
        read_lambda: |-
          // Alarm Register 14
          if (!id(feat_passthrough).state) return {};
          float v = id(alarm_reg_4d).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v);
      - address: 0x004E
        value_type: U_WORD
        read_lambda: |-
          // Alarm Register 15
          if (!id(feat_passthrough).state) return {};
          float v = id(alarm_reg_4e).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v);
      - address: 0x004F
        value_type: U_WORD
        read_lambda: |-
          // Status Register
          if (!id(feat_passthrough).state) return {};
          float v = id(alarm_reg_4f).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v);
      - address: 0x0050
        value_type: U_WORD
        read_lambda: |-
          // Controller Clock Minutes Raw
          if (!id(feat_passthrough).state) return {};
          float v = id(clock_minutes_raw).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v);
      - address: 0x0051
        value_type: U_WORD
        read_lambda: |-
          // Controller Clock Hours Raw
          if (!id(feat_passthrough).state) return {};
          float v = id(clock_hours_raw).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v);
      - address: 0x0052
        value_type: U_WORD
        read_lambda: |-
          // Controller Date MonthDay Raw
          if (!id(feat_passthrough).state) return {};
          float v = id(date_monthday_raw).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v);
      - address: 0x0053
        value_type: U_WORD
        read_lambda: |-
          // Controller Year Raw
          if (!id(feat_passthrough).state) return {};
          float v = id(date_year_raw).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v);
      - address: 0x0054
        value_type: U_WORD
        read_lambda: |-
          // Nominal kW
          if (!id(feat_passthrough).state) return {};
          float v = id(nominal_kw).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v / 0.00390625f);
      - address: 0x0055
        value_type: U_WORD
        read_lambda: |-
          // Controller Firmware
          if (!id(feat_passthrough).state) return {};
          float v = id(controller_firmware).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v);
      - address: 0x0056
        value_type: U_WORD
        read_lambda: |-
          // Unknown Register 0x0056
          if (!id(feat_passthrough).state) return {};
          float v = id(unknown_register_0x0056).state;
          if (std::isnan(v)) return (uint16_t) 0xFFFF;
          return (uint16_t) lroundf(v);
