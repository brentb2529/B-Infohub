# GENERATED by contract/gen.py -- DO NOT EDIT. Edit contract/telemetry.py.
#
# Writes every alarm transition to the flash event log, so a fault that
# asserts and clears while Home Assistant is unreachable still leaves a record.
# That is the whole point: a numeric sensor loses resolution during an outage,
# but a one-shot alarm loses the only evidence it ever happened.
#
# Polled as a single bitmask diff rather than 32 separate on_state hooks --
# one place to reason about, and regenerating the data packages cannot quietly
# remove it.
#
# The FIRST pass only primes the previous-state array. Without that, every
# asserted alarm would be written as a fresh transition on each boot and the
# log would fill with events that did not happen.

interval:
  - interval: 1s
    then:
      - lambda: |-
          // GENERATED by contract/gen.py -- do not edit here.
          static bool prev[32];
          static bool primed = false;
          if (!id(bus_healthy).state) { primed = false; return; }
          bool cur[32];
          cur[0] = id(common_shutdown).state;
          cur[1] = id(low_oil_pressure).state;
          cur[2] = id(high_coolant_temperature).state;
          cur[3] = id(engine_under_speed).state;
          cur[4] = id(engine_over_speed).state;
          cur[5] = id(engine_failed_to_start).state;
          cur[6] = id(engine_failed_to_stop).state;
          cur[7] = id(generator_low_frequency).state;
          cur[8] = id(generator_high_frequency).state;
          cur[9] = id(generator_high_current).state;
          cur[10] = id(generator_overload).state;
          cur[11] = id(unbalanced_load).state;
          cur[12] = id(emergency_stop).state;
          cur[13] = id(battery_low_voltage).state;
          cur[14] = id(battery_high_voltage).state;
          cur[15] = id(temperature_circuit_open).state;
          cur[16] = id(magnetic_pick_up_fault).state;
          cur[17] = id(gen_l1_phase_low_voltage).state;
          cur[18] = id(gen_l1_phase_high_voltage).state;
          cur[19] = id(gen_l2_phase_low_voltage).state;
          cur[20] = id(gen_l2_phase_high_voltage).state;
          cur[21] = id(high_oil_pressure_detected).state;
          cur[22] = id(alternator_input_lost).state;
          cur[23] = id(high_voltage_warning_ph_ph).state;
          cur[24] = id(high_voltage_warning_1_ph_ph).state;
          cur[25] = id(high_voltage_warning_2_ph_ph).state;
          cur[26] = id(low_voltage_failure_1_ph_ph).state;
          cur[27] = id(low_voltage_failure_2_ph_ph).state;
          cur[28] = id(generator_failed_to_start).state;
          cur[29] = id(general_notification).state;
          cur[30] = id(general_warning).state;
          cur[31] = id(electrical_trip).state;
          if (!primed) { for (int k=0;k<32;k++) prev[k]=cur[k]; primed=true; return; }
          for (int k = 0; k < 32; k++) {
            if (cur[k] == prev[k]) continue;
            backlog::global_backlog->record(
                cur[k] ? backlog::EVENT_ALARM_SET : backlog::EVENT_ALARM_CLEAR, k);
            prev[k] = cur[k];
          }
