# GENERATED by contract/gen.py -- DO NOT EDIT. Edit contract/telemetry.py.
#
# One retained JSON snapshot of everything the bridge can see, for the
# ha-energytrak custom component to consume as its LOCAL source.
#
# Published to ${mqtt_prefix}/telemetry every 10s, retained, so a
# restarting Home Assistant has current state immediately rather than after a
# poll interval. Availability comes from the birth/will on ${mqtt_prefix}/status
# already configured in packages/mqtt.yaml.
#
# The integration must check `bus_healthy` before trusting the values: a bridge
# whose RS-485 has died is still online and still retaining this document, and
# its stale readings look exactly like an idle generator.

interval:
  - interval: 10s
    then:
      - if:
          condition:
            lambda: 'return id(feat_mqtt).state;'
          then:
            - mqtt.publish_json:
                topic: ${mqtt_prefix}/telemetry
                retain: true
                payload: |-
                  // GENERATED by contract/gen.py -- do not edit here.
                  int _ac = 0; std::string _al;
                  if (id(alternator_input_lost).state) { _ac++; if (!_al.empty()) _al += ", "; _al += "Alternator Input Lost"; }
                  if (id(battery_high_voltage).state) { _ac++; if (!_al.empty()) _al += ", "; _al += "Battery High Voltage"; }
                  if (id(battery_low_voltage).state) { _ac++; if (!_al.empty()) _al += ", "; _al += "Battery Low Voltage"; }
                  if (id(common_shutdown).state) { _ac++; if (!_al.empty()) _al += ", "; _al += "Common Shutdown"; }
                  if (id(electrical_trip).state) { _ac++; if (!_al.empty()) _al += ", "; _al += "Electrical Trip"; }
                  if (id(emergency_stop).state) { _ac++; if (!_al.empty()) _al += ", "; _al += "Emergency Stop"; }
                  if (id(engine_failed_to_start).state) { _ac++; if (!_al.empty()) _al += ", "; _al += "Engine Failed to Start"; }
                  if (id(engine_failed_to_stop).state) { _ac++; if (!_al.empty()) _al += ", "; _al += "Engine Failed to Stop"; }
                  if (id(engine_over_speed).state) { _ac++; if (!_al.empty()) _al += ", "; _al += "Engine Over Speed"; }
                  if (id(engine_under_speed).state) { _ac++; if (!_al.empty()) _al += ", "; _al += "Engine Under Speed"; }
                  if (id(gen_l1_phase_high_voltage).state) { _ac++; if (!_al.empty()) _al += ", "; _al += "Gen L1 Phase High Voltage"; }
                  if (id(gen_l1_phase_low_voltage).state) { _ac++; if (!_al.empty()) _al += ", "; _al += "Gen L1 Phase Low Voltage"; }
                  if (id(gen_l2_phase_high_voltage).state) { _ac++; if (!_al.empty()) _al += ", "; _al += "Gen L2 Phase High Voltage"; }
                  if (id(gen_l2_phase_low_voltage).state) { _ac++; if (!_al.empty()) _al += ", "; _al += "Gen L2 Phase Low Voltage"; }
                  if (id(general_notification).state) { _ac++; if (!_al.empty()) _al += ", "; _al += "General Notification"; }
                  if (id(general_warning).state) { _ac++; if (!_al.empty()) _al += ", "; _al += "General Warning"; }
                  if (id(generator_failed_to_start).state) { _ac++; if (!_al.empty()) _al += ", "; _al += "Generator Failed to Start"; }
                  if (id(generator_high_current).state) { _ac++; if (!_al.empty()) _al += ", "; _al += "Generator High Current"; }
                  if (id(generator_high_frequency).state) { _ac++; if (!_al.empty()) _al += ", "; _al += "Generator High Frequency"; }
                  if (id(generator_low_frequency).state) { _ac++; if (!_al.empty()) _al += ", "; _al += "Generator Low Frequency"; }
                  if (id(generator_overload).state) { _ac++; if (!_al.empty()) _al += ", "; _al += "Generator Overload"; }
                  if (id(high_coolant_temperature).state) { _ac++; if (!_al.empty()) _al += ", "; _al += "High Coolant Temperature"; }
                  if (id(high_oil_pressure_detected).state) { _ac++; if (!_al.empty()) _al += ", "; _al += "High Oil Pressure Detected"; }
                  if (id(high_voltage_warning_1_ph_ph).state) { _ac++; if (!_al.empty()) _al += ", "; _al += "High Voltage Warning 1 (Ph-Ph)"; }
                  if (id(high_voltage_warning_2_ph_ph).state) { _ac++; if (!_al.empty()) _al += ", "; _al += "High Voltage Warning 2 (Ph-Ph)"; }
                  if (id(high_voltage_warning_ph_ph).state) { _ac++; if (!_al.empty()) _al += ", "; _al += "High Voltage Warning (Ph-Ph)"; }
                  if (id(low_oil_pressure).state) { _ac++; if (!_al.empty()) _al += ", "; _al += "Low Oil Pressure"; }
                  if (id(low_voltage_failure_1_ph_ph).state) { _ac++; if (!_al.empty()) _al += ", "; _al += "Low Voltage Failure 1 (Ph-Ph)"; }
                  if (id(low_voltage_failure_2_ph_ph).state) { _ac++; if (!_al.empty()) _al += ", "; _al += "Low Voltage Failure 2 (Ph-Ph)"; }
                  if (id(magnetic_pick_up_fault).state) { _ac++; if (!_al.empty()) _al += ", "; _al += "Magnetic Pick Up Fault"; }
                  if (id(temperature_circuit_open).state) { _ac++; if (!_al.empty()) _al += ", "; _al += "Temperature Circuit Open"; }
                  if (id(unbalanced_load).state) { _ac++; if (!_al.empty()) _al += ", "; _al += "Unbalanced Load"; }
                  
                  root["telemetry_source"] = "local";
                  root["schema"] = 1;
                  root["bus_healthy"] = id(bus_healthy).state;
                  root["bus_age_s"] = id(bus_age_seconds).state;
                  root["uptime_s"] = (uint32_t)(millis()/1000);
                  
                  // --- keys ha-energytrak already knows (same names as the cloud path) ---
                  if (!std::isnan(id(battery_voltage).state)) root["battery_voltage"] = id(battery_voltage).state;
                  if (!std::isnan(id(engine_speed).state)) root["engine_speed"] = id(engine_speed).state;
                  if (!std::isnan(id(engine_run_time).state)) root["engine_hours"] = id(engine_run_time).state;
                  if (!std::isnan(id(number_of_starts).state)) root["starts_count"] = id(number_of_starts).state;
                  if (!std::isnan(id(number_of_trips).state)) root["trips_count"] = id(number_of_trips).state;
                  if (!std::isnan(id(generator_l1_frequency).state)) root["generator_frequency"] = id(generator_l1_frequency).state;
                  if (!std::isnan(id(utility_l1_frequency).state)) root["grid_frequency"] = id(utility_l1_frequency).state;
                  if (!std::isnan(id(utility_l1_l2_voltage).state)) root["grid_voltage"] = id(utility_l1_l2_voltage).state;
                  if (!std::isnan(id(generator_l1_voltage).state)) root["output_voltage_l1n"] = id(generator_l1_voltage).state;
                  if (!std::isnan(id(generator_l2_voltage).state)) root["output_voltage_l2n"] = id(generator_l2_voltage).state;
                  if (!std::isnan(id(generator_l1_current).state)) root["load_l1_current"] = id(generator_l1_current).state;
                  if (!std::isnan(id(generator_l2_current).state)) root["load_l2_current"] = id(generator_l2_current).state;
                  if (!std::isnan(id(l1_power).state)) root["load_l1_power"] = id(l1_power).state;
                  if (!std::isnan(id(l2_power).state)) root["load_l2_power"] = id(l2_power).state;
                  if (!std::isnan(id(total_power).state)) root["load_power"] = id(total_power).state;
                  if (!std::isnan(id(l1_apparent_power).state)) root["load_l1_apparent_power"] = id(l1_apparent_power).state;
                  if (!std::isnan(id(l2_apparent_power).state)) root["load_l2_apparent_power"] = id(l2_apparent_power).state;
                  if (!std::isnan(id(total_apparent_power).state)) root["load_apparent_power"] = id(total_apparent_power).state;
                  if (!std::isnan(id(l1_reactive_power).state)) root["load_l1_reactive_power"] = id(l1_reactive_power).state;
                  if (!std::isnan(id(l2_reactive_power).state)) root["load_l2_reactive_power"] = id(l2_reactive_power).state;
                  if (!std::isnan(id(total_reactive_power).state)) root["load_reactive_power"] = id(total_reactive_power).state;
                  if (!std::isnan(id(average_power_factor).state)) root["power_factor"] = id(average_power_factor).state;
                  if (!std::isnan(id(l1_power_factor).state)) root["power_factor_l1"] = id(l1_power_factor).state;
                  if (!std::isnan(id(l2_power_factor).state)) root["power_factor_l2"] = id(l2_power_factor).state;
                  if (!id(switch_status).state.empty()) root["operation_mode"] = id(switch_status).state;
                  if (!id(engine_state).state.empty()) root["ignition_status"] = id(engine_state).state;
                  root["fault_condition"] = id(common_shutdown).state;
                  if (!id(last_exercise_at).state.empty()) root["last_exercise_at"] = id(last_exercise_at).state;
                  if (!std::isnan(id(last_exercise_duration_seconds).state)) root["last_exercise_duration_seconds"] = id(last_exercise_duration_seconds).state;
                  
                  // --- derived ---
                  root["grid_present"] = !id(utility_power_failure).state;
                  root["grid_status"] = (id(utility_power_failure).state ? "Lost" : "Present");
                  root["active_alarm_count"] = _ac;
                  root["active_alarms"] = _al.empty() ? "None" : _al.c_str();
                  root["has_malfunction"] = id(common_shutdown).state;
                  root["monitor_online"] = true;
                  
                  // --- local-only superset: 118 keys the cloud never had ---
                  if (!std::isnan(id(alarm_reg_40).state)) root["alarm_register_1"] = id(alarm_reg_40).state;
                  if (!std::isnan(id(alarm_reg_41).state)) root["alarm_register_2"] = id(alarm_reg_41).state;
                  if (!std::isnan(id(alarm_reg_42).state)) root["alarm_register_3"] = id(alarm_reg_42).state;
                  if (!std::isnan(id(alarm_reg_43).state)) root["alarm_register_4"] = id(alarm_reg_43).state;
                  if (!std::isnan(id(alarm_reg_44).state)) root["alarm_register_5"] = id(alarm_reg_44).state;
                  if (!std::isnan(id(alarm_reg_45).state)) root["alarm_register_6"] = id(alarm_reg_45).state;
                  if (!std::isnan(id(alarm_reg_46).state)) root["alarm_register_7"] = id(alarm_reg_46).state;
                  if (!std::isnan(id(alarm_reg_47).state)) root["alarm_register_8"] = id(alarm_reg_47).state;
                  if (!std::isnan(id(alarm_reg_48).state)) root["alarm_register_9"] = id(alarm_reg_48).state;
                  if (!std::isnan(id(alarm_reg_49).state)) root["alarm_register_10"] = id(alarm_reg_49).state;
                  if (!std::isnan(id(alarm_reg_4a).state)) root["alarm_register_11"] = id(alarm_reg_4a).state;
                  if (!std::isnan(id(alarm_reg_4b).state)) root["alarm_register_12"] = id(alarm_reg_4b).state;
                  if (!std::isnan(id(alarm_reg_4c).state)) root["alarm_register_13"] = id(alarm_reg_4c).state;
                  if (!std::isnan(id(alarm_reg_4d).state)) root["alarm_register_14"] = id(alarm_reg_4d).state;
                  if (!std::isnan(id(alarm_reg_4e).state)) root["alarm_register_15"] = id(alarm_reg_4e).state;
                  if (!std::isnan(id(alarm_reg_4f).state)) root["status_register"] = id(alarm_reg_4f).state;
                  root["alternator_input_lost"] = id(alternator_input_lost).state;
                  root["auxiliary_input_a"] = id(auxiliary_input_a).state;
                  root["auxiliary_input_b"] = id(auxiliary_input_b).state;
                  root["auxiliary_input_c"] = id(auxiliary_input_c).state;
                  root["auxiliary_input_d"] = id(auxiliary_input_d).state;
                  root["auxiliary_input_e"] = id(auxiliary_input_e).state;
                  root["auxiliary_input_f"] = id(auxiliary_input_f).state;
                  root["auxiliary_input_g"] = id(auxiliary_input_g).state;
                  root["auxiliary_input_h"] = id(auxiliary_input_h).state;
                  root["auxiliary_input_i"] = id(auxiliary_input_i).state;
                  if (!std::isnan(id(backlog_count).state)) root["event_log_records"] = id(backlog_count).state;
                  if (!std::isnan(id(backlog_cursor).state)) root["event_log_cursor"] = id(backlog_cursor).state;
                  if (!std::isnan(id(backlog_pending).state)) root["event_log_pending"] = id(backlog_pending).state;
                  root["battery_high_voltage"] = id(battery_high_voltage).state;
                  root["battery_low_voltage"] = id(battery_low_voltage).state;
                  if (!id(boot_partition).state.empty()) root["boot_partition"] = id(boot_partition).state;
                  if (!std::isnan(id(bridge_uptime).state)) root["uptime"] = id(bridge_uptime).state;
                  if (!std::isnan(id(bus_reads_missed).state)) root["bus_reads_missed"] = id(bus_reads_missed).state;
                  if (!std::isnan(id(bus_reads_ok).state)) root["bus_reads_ok"] = id(bus_reads_ok).state;
                  if (!std::isnan(id(bus_success_rate).state)) root["bus_success_rate"] = id(bus_success_rate).state;
                  if (!std::isnan(id(bus_witness).state)) root["bus_witness"] = id(bus_witness).state;
                  if (!std::isnan(id(controller_clock_age).state)) root["controller_clock_age"] = id(controller_clock_age).state;
                  if (!std::isnan(id(controller_clock_skew).state)) root["controller_clock_skew"] = id(controller_clock_skew).state;
                  if (!id(controller_time).state.empty()) root["controller_time"] = id(controller_time).state;
                  if (!std::isnan(id(coolant_temperature).state)) root["coolant_temperature"] = id(coolant_temperature).state;
                  if (!std::isnan(id(cumulative_apparent_energy).state)) root["cumulative_apparent_energy"] = id(cumulative_apparent_energy).state;
                  if (!std::isnan(id(cumulative_energy).state)) root["cumulative_energy"] = id(cumulative_energy).state;
                  if (!std::isnan(id(cumulative_reactive_energy).state)) root["cumulative_reactive_energy"] = id(cumulative_reactive_energy).state;
                  root["dg_phase_rotation"] = id(dg_phase_rotation).state;
                  root["electrical_trip"] = id(electrical_trip).state;
                  root["emergency_stop"] = id(emergency_stop).state;
                  root["engine_failed_to_start"] = id(engine_failed_to_start).state;
                  root["engine_failed_to_stop"] = id(engine_failed_to_stop).state;
                  root["engine_over_speed"] = id(engine_over_speed).state;
                  root["engine_running"] = id(engine_running).state;
                  root["engine_starting"] = id(engine_starting).state;
                  root["engine_under_speed"] = id(engine_under_speed).state;
                  if (!std::isnan(id(esp_temp).state)) root["esp_internal_temperature"] = id(esp_temp).state;
                  root["exercising"] = id(exercising).state;
                  root["extended_over_load_trip"] = id(extended_over_load_trip).state;
                  if (!std::isnan(id(free_heap).state)) root["free_heap"] = id(free_heap).state;
                  root["fuel_cap_open_circuit"] = id(fuel_cap_open_circuit).state;
                  if (!std::isnan(id(fuel_level).state)) root["fuel_level"] = id(fuel_level).state;
                  root["fuel_theft"] = id(fuel_theft).state;
                  if (!std::isnan(id(fuel_volume).state)) root["fuel_volume"] = id(fuel_volume).state;
                  root["gen_l1_phase_high_voltage"] = id(gen_l1_phase_high_voltage).state;
                  root["gen_l1_phase_low_voltage"] = id(gen_l1_phase_low_voltage).state;
                  root["gen_l2_phase_high_voltage"] = id(gen_l2_phase_high_voltage).state;
                  root["gen_l2_phase_low_voltage"] = id(gen_l2_phase_low_voltage).state;
                  root["gen_l3_phase_high_voltage"] = id(gen_l3_phase_high_voltage).state;
                  root["gen_l3_phase_low_voltage"] = id(gen_l3_phase_low_voltage).state;
                  root["general_notification"] = id(general_notification).state;
                  root["general_warning"] = id(general_warning).state;
                  root["generator_failed_to_start"] = id(generator_failed_to_start).state;
                  root["generator_high_current"] = id(generator_high_current).state;
                  root["generator_high_frequency"] = id(generator_high_frequency).state;
                  if (!std::isnan(id(generator_l1_l2_voltage).state)) root["generator_l1-l2_voltage"] = id(generator_l1_l2_voltage).state;
                  if (!std::isnan(id(generator_l2_frequency).state)) root["generator_l2_frequency"] = id(generator_l2_frequency).state;
                  if (!std::isnan(id(generator_l2_l3_voltage).state)) root["generator_l2-l3_voltage"] = id(generator_l2_l3_voltage).state;
                  if (!std::isnan(id(generator_l3_current).state)) root["generator_l3_current"] = id(generator_l3_current).state;
                  if (!std::isnan(id(generator_l3_frequency).state)) root["generator_l3_frequency"] = id(generator_l3_frequency).state;
                  if (!std::isnan(id(generator_l3_l1_voltage).state)) root["generator_l3-l1_voltage"] = id(generator_l3_l1_voltage).state;
                  if (!std::isnan(id(generator_l3_voltage).state)) root["generator_l3_voltage"] = id(generator_l3_voltage).state;
                  root["generator_low_frequency"] = id(generator_low_frequency).state;
                  root["generator_overload"] = id(generator_overload).state;
                  if (!std::isnan(id(heap_max_block).state)) root["largest_free_block"] = id(heap_max_block).state;
                  root["high_coolant_temperature"] = id(high_coolant_temperature).state;
                  root["high_oil_pressure_detected"] = id(high_oil_pressure_detected).state;
                  root["high_voltage_warning_1__ph-ph_"] = id(high_voltage_warning_1_ph_ph).state;
                  root["high_voltage_warning_2__ph-ph_"] = id(high_voltage_warning_2_ph_ph).state;
                  root["high_voltage_warning__ph-ph_"] = id(high_voltage_warning_ph_ph).state;
                  if (!std::isnan(id(l3_apparent_power).state)) root["l3_apparent_power"] = id(l3_apparent_power).state;
                  if (!std::isnan(id(l3_power).state)) root["l3_power"] = id(l3_power).state;
                  if (!std::isnan(id(l3_power_factor).state)) root["l3_power_factor"] = id(l3_power_factor).state;
                  if (!std::isnan(id(l3_reactive_power).state)) root["l3_reactive_power"] = id(l3_reactive_power).state;
                  if (!id(last_reset_reason).state.empty()) root["last_reset_reason"] = id(last_reset_reason).state;
                  if (!std::isnan(id(loop_time).state)) root["loop_time"] = id(loop_time).state;
                  root["low_fuel_level"] = id(low_fuel_level).state;
                  root["low_oil_pressure"] = id(low_oil_pressure).state;
                  root["low_voltage_failure_1__ph-ph_"] = id(low_voltage_failure_1_ph_ph).state;
                  root["low_voltage_failure_2__ph-ph_"] = id(low_voltage_failure_2_ph_ph).state;
                  root["magnetic_pick_up_fault"] = id(magnetic_pick_up_fault).state;
                  root["mains_phase_rotation"] = id(mains_phase_rotation).state;
                  root["maintenance_required"] = id(maintenance_required).state;
                  if (!std::isnan(id(oil_pressure_bar).state)) root["oil_pressure"] = id(oil_pressure_bar).state;
                  root["oil_pressure_open_circuit"] = id(oil_pressure_open_circuit).state;
                  if (!std::isnan(id(percentage_load).state)) root["percentage_load"] = id(percentage_load).state;
                  if (!id(power_status).state.empty()) root["power_status"] = id(power_status).state;
                  root["scheduled_exercise_in_progress"] = id(scheduled_exercise).state;
                  if (!id(status_register_raw).state.empty()) root["status_register_raw"] = id(status_register_raw).state;
                  root["temperature_circuit_open"] = id(temperature_circuit_open).state;
                  root["unbalanced_load"] = id(unbalanced_load).state;
                  if (!std::isnan(id(utility_l1_voltage).state)) root["utility_l1_voltage"] = id(utility_l1_voltage).state;
                  if (!std::isnan(id(utility_l2_frequency).state)) root["utility_l2_frequency"] = id(utility_l2_frequency).state;
                  if (!std::isnan(id(utility_l2_l3_voltage).state)) root["utility_l2-l3_voltage"] = id(utility_l2_l3_voltage).state;
                  if (!std::isnan(id(utility_l2_voltage).state)) root["utility_l2_voltage"] = id(utility_l2_voltage).state;
                  if (!std::isnan(id(utility_l3_frequency).state)) root["utility_l3_frequency"] = id(utility_l3_frequency).state;
                  if (!std::isnan(id(utility_l3_l1_voltage).state)) root["utility_l3-l1_voltage"] = id(utility_l3_l1_voltage).state;
                  if (!std::isnan(id(utility_l3_voltage).state)) root["utility_l3_voltage"] = id(utility_l3_voltage).state;
                  root["utility_power_failure"] = id(utility_power_failure).state;
                  root["water_level_switch"] = id(water_level_switch).state;
                  if (!std::isnan(id(wifi_rssi).state)) root["wi-fi_signal"] = id(wifi_rssi).state;
